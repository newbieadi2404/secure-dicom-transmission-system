# Backend Fix Progress

## Steps:
- [x] 1. Update backend/app.py: Add sys.path.insert for import fix, remove duplicate blueprint reg.\n  Edit complete, routes ok (no bad imports).
- [ ] 2. Test: cd .. && python backend/app.py (should start server without ModuleNotFound).
- [ ] 3. If routes fail (similar imports), fix them.
- [ ] 4. pip install -r ../requirements.txt (from backend/)
- [ ] 5. Test endpoints, DB init.
- [x] Plan approved.
